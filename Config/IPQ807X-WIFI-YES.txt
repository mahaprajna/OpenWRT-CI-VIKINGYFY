# ============================================================
# 目标平台：Qualcomm IPQ807x
# ============================================================
CONFIG_TARGET_qualcommax=y
CONFIG_TARGET_qualcommax_ipq807x=y

# ============================================================
# 设备列表：仅保留小米 AX3600（删除其他所有设备）
# ============================================================
# 注意：默认使用大分区布局，不要同时勾选 -stock 版本
CONFIG_TARGET_DEVICE_qualcommax_ipq807x_DEVICE_xiaomi_ax3600=y

# ============================================================
# NSS 硬件加速固件与驱动（VIKINGYFY main 分支已内置，
# 以下为参考项，通常无需手动添加）
# ============================================================
# CONFIG_PACKAGE_nss-eip-firmware=y
# CONFIG_PACKAGE_nss-firmware-ipq8074=y
# CONFIG_PACKAGE_kmod-qca-nss-dp=y
# CONFIG_PACKAGE_kmod-qca-nss-drv=y
# CONFIG_PACKAGE_kmod-qca-nss-ecm=y

# ============================================================
# 常用 LuCI 插件（按需启用，取消注释即可）
# ============================================================
# 科学上网工具（二选一，不要同时开启）
# CONFIG_PACKAGE_luci-app-openclash=y
CONFIG_PACKAGE_luci-app-homeproxy=y

# ============================================================
# DNS 优化：SmartDNS 轻量版（替代完整版，资源占用更低）
# 提供 DNS 测速优选、域名分流、家长管理、加密 DoH/DoT
# ============================================================
# CONFIG_PACKAGE_luci-app-smartdns-lite=y
# CONFIG_PACKAGE_luci-i18n-smartdns-lite-zh-cn=y
# CONFIG_PACKAGE_smartdns=y

# DNS 加密（DoH）
CONFIG_PACKAGE_https-dns-proxy=y
CONFIG_PACKAGE_luci-app-https-dns-proxy=y
CONFIG_PACKAGE_luci-i18n-https-dns-proxy-zh-cn=y

# 网络工具
CONFIG_PACKAGE_luci-app-upnp=y
